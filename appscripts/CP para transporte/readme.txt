Script para generar una busqueda de datos de pedidos a partir de  los reportes de Consulta de pedidos cargados en una carpeta, se indexan los pedidos en otra carpeta por grupos hasta 100mil filas

una vez indexados la busqueda se hace en la carpeta de indexacion donde indica el id y fila donde esta el pedido donde busca el campo solicitado del encabezado

        const valorPracticado = datosFila[12];
        const valorProductos = datosFila[14];
        const fechaAutorizacion = datosFila[24];

Consolidacion de archivos de query CP Gera en drive para indexar